initialize() {
	initializeTreeSeq(simplificationRatio=200.0);
	initializeMutationRate(0);
	initializeMutationType("m1", 0.5, "f", 0.0);
	initializeGenomicElementType("g1", m1, 1);
	initializeGenomicElement(g1, 0, 1e8 - 1);
	initializeRecombinationRate(2e-9);
}
1 {
	sim.addSubpop("p1", 100000);
}
1: late() {
	p1.individuals.fitnessScaling = 1.0 / rgamma(p1.individualCount, 1.0, exp((1001.0 - sim.generation) / 500.0));
}
1000 late() {
	sim.treeSeqOutput("./ex4_TS_decap.trees");
}
